<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <!-- Android network security config does not support CIDR private-LAN ranges.
         Keep release cleartext scoped to loopback/emulator and the known self-hosted
         Hermes LAN endpoint used for physical-device verification. Runtime OkHttp
         policy also rejects any non-local cleartext host. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">192.168.0.157</domain>
    </domain-config>
</network-security-config>
